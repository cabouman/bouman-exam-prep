window.BIGPICTURE = window.BIGPICTURE || {};
(function(){
const R = String.raw; const B = window.BIGPICTURE;
B["ece641/midterm/map"] = R`<p><b>Why this exists.</b> Every imaging system produces data that under-determine the image: too few measurements, noise, blur. Inverse filtering fails because it amplifies whatever the data cannot see. MAP estimation is the course's answer: combine what the data say (the likelihood) with what images are like (the prior) in one cost function, then optimize. Almost everything else in the course is a refinement of this one move.</p>
<p><b>Where it sits.</b> It builds on ML estimation and Gaussian models; it feeds directly into surrogate functions and ICD (how to minimize the cost), GMRF priors (what to use as the prior), and proximal maps and PnP (how to swap the prior for a learned denoiser).</p>
<p><b>The one idea to carry away.</b> An estimate is a compromise: minimize (misfit to data) plus (implausibility), and the algebra of Gaussians makes that compromise a linear system you can solve or iterate.</p>
<p><b>Where you meet it in practice.</b> CT and MRI reconstruction, deblurring, denoising, super-resolution, and the loss-plus-regularizer structure of nearly every machine-learning objective.</p>`;
B["ece641/midterm/prox"] = R`<p><b>Why this exists.</b> Real priors are not quadratic (edges, sparsity, positivity) and modern priors are neural networks with no formula at all. Proximal maps package "a small optimization against a quadratic tether" into a reusable operator, and ADMM shows that a hard problem splits into easy proximal steps that only talk through a consensus variable. Plug-and-play is the payoff: the prior step is literally a denoiser, so any denoiser, including a trained one, can serve as a prior.</p>
<p><b>Where it sits.</b> It assumes MAP estimation and convexity; it generalizes surrogate methods (a proximal step is a surrogate minimization) and leads to the final's PnP, consensus equilibrium and fixed-point theory.</p>
<p><b>The one idea to carry away.</b> Separate the data physics from the image prior into two agents and let them negotiate; convergence follows from non-expansiveness, not from having a formula for the prior.</p>
<p><b>Where you meet it in practice.</b> Sparse reconstruction, compressed-sensing MRI, deep-learning-based CT reconstruction, and distributed optimization in general.</p>`;
B["ece641/midterm/mm"] = R`<p><b>Why this exists.</b> Robust and non-Gaussian priors give costs you cannot minimize in closed form and whose Newton steps are too expensive at image scale. Majorization–minimization replaces the cost at each step by a simpler function that lies above it and touches it; minimizing the simple function is guaranteed not to increase the true cost. It is the engine behind ICD with robust priors, separable parallel updates, and EM.</p>
<p><b>Where it sits.</b> Needs convexity and MAP; explains why EM works (the Q-function is a surrogate) and why plug-and-play steps decrease a cost.</p>
<p><b>The one idea to carry away.</b> If you cannot minimize $f$, minimize something that upper-bounds $f$ and touches it at your current point; repeat. Monotone progress is free.</p>
<p><b>Where you meet it in practice.</b> Iteratively reweighted least squares, tomographic reconstruction on GPUs, EM for mixtures, and many training heuristics in machine learning.</p>`;
B["ece641/midterm/gmrf"] = R`<p><b>Why this exists.</b> A prior for images must say that neighboring pixels are similar without saying much else. Gaussian Markov random fields do this with a sparse precision matrix: each pixel is predicted from its neighbors, and the whole field is still jointly Gaussian so MAP estimation stays a linear problem. The causal AR view connects to classical signal processing; the non-causal view is what images actually need.</p>
<p><b>Where it sits.</b> Builds on power spectra and AR processes from the prerequisite course; supplies the $B$ matrix in every quadratic MAP problem; generalizes to non-Gaussian MRFs on the final.</p>
<p><b>The one idea to carry away.</b> Zeros in the precision matrix are conditional independences; the prediction filter and the precision matrix are the same object written two ways.</p>
<p><b>Where you meet it in practice.</b> Regularizers in reconstruction, texture modeling, spatial statistics, and graphical models in machine learning.</p>`;
B["ece641/midterm/ml"] = R`<p><b>Why this exists.</b> Before you can put a prior on an image you must be able to estimate parameters from data at all. Maximum likelihood is the baseline: choose the parameter that makes the data most probable. Its mechanics (log-likelihood, sufficient statistics, Lagrange multipliers for constraints, bias and variance) recur in every later chapter, and its weaknesses (variance, ill-posedness) are what motivate MAP.</p>
<p><b>Where it sits.</b> First tool in the course; MAP is ML plus a prior; EM is ML with hidden data; PnP training minimizes a likelihood-derived loss.</p>
<p><b>The one idea to carry away.</b> Write the negative log-likelihood, find the sufficient statistics, differentiate; the estimate is usually a sample average, and its variance shrinks like one over the sample size.</p>
<p><b>Where you meet it in practice.</b> Calibrating noise models, fitting prior parameters, training neural networks (cross-entropy and MSE are negative log-likelihoods).</p>`;
B["ece641/midterm/convex"] = R`<p><b>Why this exists.</b> An algorithm is only worth running if the problem it solves has a solution and if the solution is the one you want. Convexity is the property that makes local search global, and compactness arguments are what guarantee a minimizer exists at all. Knowing these proofs tells you when an estimator is well defined and when regularization is needed to make it so.</p>
<p><b>Where it sits.</b> Underpins MAP, proximal maps (existence and uniqueness of the prox), and the convergence arguments of PnP.</p>
<p><b>The one idea to carry away.</b> Existence comes from coercivity plus continuity; uniqueness comes from strict convexity; when either is missing, add a small quadratic term.</p>
<p><b>Where you meet it in practice.</b> Choosing regularizers, diagnosing non-unique reconstructions, and reading convergence theorems in optimization papers.</p>`;
B["ece641/midterm/gibbs"] = R`<p><b>Why this exists.</b> Priors written as $\exp\{-u(x)\}/z$ are convenient except for $z$, which is usually uncomputable. Knowing how $z$ scales with a parameter lets you fit prior parameters by ML anyway. And the Gaussian case, where MMSE and MAP coincide, is the reference point for understanding what trained denoisers (MMSE) compute versus what optimization (MAP) computes.</p>
<p><b>Where it sits.</b> Connects MAP estimation to the MRF chapter and to the PnP discussion of MAP versus MMSE denoisers.</p>
<p><b>The one idea to carry away.</b> For Gaussians the mean is the mode, so estimation is linear; for anything else the two differ and you must decide which you want.</p>
<p><b>Where you meet it in practice.</b> Hyperparameter estimation for regularizers and the interpretation of learned denoisers.</p>`;
B["ece641/midterm/words"] = R`<p><b>Why this exists.</b> Equations are compressed arguments. The course asks you to decompress them because engineers who can say what an equation means can adapt it to a new problem, while those who only manipulate symbols cannot.</p>
<p><b>Where it sits.</b> Cuts across every chapter; the most-tested equations are MAP, the surrogate inequality, the soft threshold and the PnP equilibrium.</p>
<p><b>The one idea to carry away.</b> Name it, explain each part, state the consequence, give an example.</p>
<p><b>Where you meet it in practice.</b> Research talks, reviews and every time you explain a method to a collaborator.</p>`;
B["ece641/final/em"] = R`<p><b>Why this exists.</b> Many models have variables you never observe: which class a pixel belongs to, which component of a mixture produced a sample. Maximizing the likelihood of the observed data alone is intractable because it sums over the hidden variables. EM turns this into an iteration of two easy steps: guess the hidden variables in expectation, then solve the complete-data ML problem. It is the workhorse for fitting mixture models, segmentation and hidden Markov models.</p>
<p><b>Where it sits.</b> Combines ML estimation, sufficient statistics and the MM idea; the Q-function is a surrogate for the log-likelihood.</p>
<p><b>The one idea to carry away.</b> For exponential families EM is "complete-data ML with expected sufficient statistics," and it never decreases the likelihood.</p>
<p><b>Where you meet it in practice.</b> Image segmentation, Gaussian mixture clustering, speech recognition training, and missing-data problems everywhere in statistics.</p>`;
B["ece641/final/mc"] = R`<p><b>Why this exists.</b> Two reasons. Markov chains model sequences with memory of one step, which is the simplest useful dependence structure. More importantly for this course, every stochastic sampler (Metropolis, Gibbs) is a Markov chain, and the theory of irreducibility, aperiodicity, ergodicity and reversibility is what guarantees the sampler produces the distribution you intended.</p>
<p><b>Where it sits.</b> Prerequisite for the sampling chapter and for HMMs; the detailed balance equations are the tool used to prove every MCMC method correct.</p>
<p><b>The one idea to carry away.</b> Finite, irreducible, aperiodic implies a unique stationary distribution that the chain forgets its start and converges to; detailed balance is the shortcut for finding it.</p>
<p><b>Where you meet it in practice.</b> MCMC in Bayesian imaging, PageRank, queueing models, and the sampling steps of diffusion models.</p>`;
B["ece641/final/pnp"] = R`<p><b>Why this exists.</b> The best image priors today are neural networks, which have no energy function. Plug-and-play reinterprets the ADMM prior step as denoising, so a trained denoiser can replace the prior, and consensus equilibrium generalizes this to any number of agents. The theory shifts from "minimize a cost" to "find a fixed point," with non-expansiveness replacing convexity as the guarantee.</p>
<p><b>Where it sits.</b> The culmination of MAP, proximal maps and ADMM; connects the model-based half of the course to deep learning.</p>
<p><b>The one idea to carry away.</b> Reconstruction is a balance between a data agent and a prior agent; equilibrium, not a formula, defines the answer.</p>
<p><b>Where you meet it in practice.</b> State-of-the-art CT, MRI and microscopy reconstruction, and research on combining physics models with learned components.</p>`;
B["ece641/final/mcmc"] = R`<p><b>Why this exists.</b> Sometimes a single MAP estimate is not enough: you want samples from the posterior to quantify uncertainty, or the prior is discrete and optimization is combinatorial. Sampling methods generate draws from a distribution known only up to a constant, by building a Markov chain whose stationary distribution is the target. Metropolis needs only energy differences; Gibbs needs only local conditionals.</p>
<p><b>Where it sits.</b> Applies Markov-chain theory to Gibbs distributions and MRFs; simulated annealing links it back to MAP estimation.</p>
<p><b>The one idea to carry away.</b> Design transitions that satisfy detailed balance with the target; the normalizing constant never appears.</p>
<p><b>Where you meet it in practice.</b> Bayesian uncertainty in imaging, statistical physics, probabilistic programming, and the samplers inside modern generative models.</p>`;
B["ece641/final/mlfinal"] = R`<p><b>Why this exists.</b> The final revisits ML because every later method depends on it: EM's M-step is complete-data ML, PnP denoisers are trained by minimizing a likelihood-derived loss, and Markov-chain parameters are fit by counting transitions. Seeing ML as "sufficient statistics plus a small optimization" is what makes the later derivations routine.</p>
<p><b>Where it sits.</b> Foundation for EM and for parameter estimation of priors and forward models.</p>
<p><b>The one idea to carry away.</b> Exponential families make ML a function of a few sums over the data; constraints are handled by Lagrange multipliers.</p>
<p><b>Where you meet it in practice.</b> Noise calibration, AR model fitting, class-frequency estimation, and every training loss you will ever minimize.</p>`;
B["ece641/final/mmfinal"] = R`<p><b>Why this exists.</b> Robust priors preserve edges but are hard to optimize; surrogate functions make them tractable by replacing them with parabolas at each step. On the final this reappears because EM and PnP are both instances of the same majorize-then-minimize logic.</p>
<p><b>Where it sits.</b> Between MAP and EM; the symmetric-bound trick is the algorithmic core of IRLS.</p>
<p><b>The one idea to carry away.</b> A weighted least-squares problem with weights from the current iterate can stand in for almost any convex potential.</p>
<p><b>Where you meet it in practice.</b> Total-variation and sparse reconstruction, robust regression, and compressive sensing solvers.</p>`;
B["ece641/final/mrf"] = R`<p><b>Why this exists.</b> Gaussian priors blur edges and cannot represent discrete labels. General Markov random fields, written as Gibbs distributions over local cliques, model both continuous images with edge-preserving potentials and discrete label fields for segmentation. The local conditional distribution is what samplers and ICM updates use.</p>
<p><b>Where it sits.</b> Generalizes GMRFs; provides the targets for Gibbs sampling and the priors for segmentation.</p>
<p><b>The one idea to carry away.</b> Global structure from local rules: a distribution defined by neighbor interactions has conditionals that depend only on neighbors.</p>
<p><b>Where you meet it in practice.</b> Image segmentation, denoising with edge preservation, texture synthesis, and graphical models in machine learning.</p>`;
B["ece641/final/icdl1"] = R`<p><b>Why this exists.</b> Sparsity is one of the most powerful priors we have, and the $\ell_1$ norm is its convex proxy. Coordinate descent with soft thresholding shows that sparsity-promoting problems can be solved with simple scalar updates, and the fixed-point argument shows why that is legitimate.</p>
<p><b>Where it sits.</b> Combines ICD with proximal maps; a stepping stone to ADMM for sparse problems.</p>
<p><b>The one idea to carry away.</b> Separable non-smooth terms make coordinate-wise optimality equal global optimality.</p>
<p><b>Where you meet it in practice.</b> LASSO regression, sparse-view CT, compressed sensing.</p>`;
B["ece641/final/genrv"] = R`<p><b>Why this exists.</b> Simulation needs random variables with prescribed distributions, and computers only provide uniforms. The inverse-CDF method is the basic conversion; whitening in reverse provides Gaussian vectors; MCMC handles everything else.</p>
<p><b>Where it sits.</b> Prerequisite for sampling methods and for generating synthetic data to train denoisers.</p>
<p><b>The one idea to carry away.</b> A monotone transformation of a uniform has whatever distribution you choose the transformation from.</p>
<p><b>Where you meet it in practice.</b> Monte Carlo simulation, generating training noise, latent codes for generative networks.</p>`;
B["ece641/final/wordsfinal"] = R`<p><b>Why this exists.</b> The final's prose question checks that the semester's central equations have become ideas you can explain, which is the form in which you will actually use them.</p>
<p><b>Where it sits.</b> Draws on Markov chains, EM and sampling in particular.</p>
<p><b>The one idea to carry away.</b> Interpretation is a skill you practice, not a summary you memorize.</p>
<p><b>Where you meet it in practice.</b> Explaining your method in a paper or a meeting.</p>`;
})();
